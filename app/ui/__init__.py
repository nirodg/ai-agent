"""UI subpackage. Page renderers and shared widgets.

Note: no public re-exports. Page boundaries are explicit; import directly from
the relevant module (e.g. `from app.ui.research_page import render_research_page`).
"""
